apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    name: recommendation-v1-route
  name: recommendation-v1-route
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: recommendation-svc
    weight: 100
  wildcardPolicy: None
